{
  "name": "fetch-spotify-artist-data",
  "version": "0.0.1",
  "description": "Fetches artist data from Spotify artist IDs",
  "main": "index.js",
  "scripts": {
    "build": "tsc src/index.ts --outDir out/",
    "watch": "tsc src/index.ts --outDir out/ --watch",
    "prerun": "npm run build",
    "run": "node out/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/node": "^14.6.0",
    "form-data": "^3.0.0",
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "axios": "^0.19.2"
  }
}
